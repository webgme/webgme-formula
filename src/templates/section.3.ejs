Containment ::= new (parent: Node, child: Node).
Set ::= new (owner: Node, member: Node, name: String).
Pointer ::= new (owner:Node, target:Node, name: String).
Attribute ::= new(name:String, owner:Node, value: String + Real + Integer).

<%
function convertName(name) {
    name = name.replace(/ /g, "_");
    name = name.replace(/\./g, "__");
    return name;
}
var nodeId = convertName(core.getPath(node)),
        keys, i, j, targets;
%> <%= nodeId %> is Node(<%= nodeId %>).
<%= nodeId %>_B_ is Inheritance(<%= core.getAttribute(core.getMetaType(node), 'name') %>, <%= nodeId %>).
<%
keys = core.getChildrenPaths(node);
for (i = 0; i < keys.length; i += 1) {
// Containment ::= new (parent: Node, child: Node).
%> <%= nodeId %>_C<%=i %> is Containment(<%= nodeId %>, <%= convertName(keys[i]) %>).
<%
}
%>